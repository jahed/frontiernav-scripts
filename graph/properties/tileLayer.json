{
  "id": "tileLayer",
  "type": "object",
  "required": [
    "folder",
    "extension"
  ],
  "properties": {
    "folder": {
      "type": "string"
    },
    "extension": {
      "type": "string",
      "enum": [
        "png",
        "jpg"
      ]
    },
    "background": {
      "$ref": "#/definitions/mapBackground"
    }
  }
}
